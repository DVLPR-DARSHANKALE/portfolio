# Portfolio

Flutter web portfolio project.

## Run locally

```powershell
flutter pub get
flutter run -d chrome
```

## Build for GitHub Pages

Use your real GitHub repository name in the base href:

```powershell
flutter build web --release --base-href /<repo-name>/
```

For full manual and automatic deployment notes, see `DEPLOY_GITHUB_PAGES.md`.
